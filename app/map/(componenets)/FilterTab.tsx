"use client";

import Button from "@/components/common/Button";

const FilterTab = () => {
  return (
    <div className="w-full flex gap-2 items-start">
      <Button text={"지역 선택"} onClick={() => {}} />
      <Button text={"앨범 선택"} onClick={() => {}} />
    </div>
  );
};
export default FilterTab;
